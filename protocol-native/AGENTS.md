# Expo HAS CHANGED

This project ships on Expo SDK 54 (`"expo": "^54.0.5"` in package.json). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not assume APIs from newer SDK releases exist — `expo-camera`, `expo-notifications`, `expo-secure-store`, and `expo-router` all changed between versions.

When bumping the SDK in the future, update this file in the same commit so contributors and AI agents stay in sync.
